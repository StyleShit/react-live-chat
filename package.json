{
  "name": "live-chat",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cd server && PORT=80 npm start",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build:client": "cd client && npm install --production && npm run build && mkdir -p ../server/public && cp -r build/* ../server/public",
    "build:server": "cd server && npm install --production",
    "build": "npm run build:client && npm run build:server"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
